## TODO
- [ ] Replace `scholars-era/frontend/index.html` with the correct finalized home page content (user requested “REPLACE EVERYTHING IN HOME PAGE”).
- [ ] Ensure required JS/CSS assets referenced by the home page exist and are loaded correctly.
- [ ] Restart backend and verify no endpoint/UI regressions (especially universities/courses homepage sections).

